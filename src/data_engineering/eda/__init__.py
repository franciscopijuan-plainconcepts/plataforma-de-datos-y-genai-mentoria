"""Exploratory Data Analysis — Excel inspection and schema inference.

This is the ONLY module (alongside `data_engineering.ingestion`) that may
import `pandas` / `openpyxl` (see `contracts/ingestion.md` boundary rules).
"""
